% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe_classification.R
\name{safe_classification}
\alias{safe_classification}
\title{安全获取物种分类树信息 (Safely Retrieve Classification Tree)}
\usage{
safe_classification(taxids, db)
}
\arguments{
\item{taxids}{字符型或数值型向量 (Character/Numeric vector)。需要查询的物种分类 ID (Taxon IDs)。}

\item{db}{字符型 (Character)。目标数据库名称，支持 \code{"ncbi"}, \code{"gbif"}, 或 \code{"bold"}。}
}
\value{
返回一个命名列表 (Named list)。列表的名称（names）即为传入的 \code{taxids}。
如果对应 ID 查询成功，列表元素将是一个包含各级分类阶元的数据框 (Data frame)；
如果查询失败或无数据，该元素将安全地赋值为 \code{NA}。
}
\description{
这是 \code{TaxStandR} 包中负责提取物种完整分类树（门、纲、目、科、属、种等）的核心网络请求模块。
本函数是对底层 \code{taxize::classification()} 的安全封装（Wrapper）。

它具备以下特性：
\enumerate{
\item 自动去重与清洗：自动过滤掉 \code{NA} 及无效的 \code{"NA"} 字符串，并剔除重复 ID，节省网络请求时间。
\item 防崩溃网络重试：无缝接入本包的 \code{auto_retry()}，即使遭遇网络中断也能自动重试。
\item UI 进度反馈：内置控制台进度条（Progress bar），在处理成千上万条数据时提供直观的耗时预期。
\item 严苛的返回值校验：严格审查 API 的返回格式，只有返回标准数据框（Data frame）才会被接纳，
否则一律安全转化为 \code{NA}，彻底杜绝畸形数据导致的后续合并崩溃。
}
}
