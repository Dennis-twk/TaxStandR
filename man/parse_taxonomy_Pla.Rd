% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_taxonomy_Pla.R
\name{parse_taxonomy_Pla}
\alias{parse_taxonomy_Pla}
\title{解析植物分类树列表为宽数据框 (Parse Plant Taxonomy List)}
\usage{
parse_taxonomy_Pla(class_obj, db_name = "NCBI")
}
\arguments{
\item{class_obj}{列表 (List)。由分类检索提取函数返回的分类阶元列表，列表的名称（names）必须为对应的 taxid。}

\item{db_name}{字符型 (Character)。指定数据库的名称前缀，默认为 \code{"NCBI"}（支持 \code{"GBIF"}, \code{"BOLD"} 等）。
生成的数据框列名将以此作为前缀（例如：\code{GBIF.kingdom}, \code{BOLD.family}）。}
}
\value{
返回一个数据框 (Data.frame)。包含 \code{taxid} 列，以及带有数据库前缀的 7 个分类阶元列。
若某个物种缺失某一层级的阶元，对应的值将自动填充为 \code{NA}。
}
\description{
本函数用于将 \code{taxize::classification()} 或自定义函数 \code{safe_classification()}
返回的复杂嵌套列表（List）扁平化，转换为宽格式（Wide format）的数据框（Data.frame）。
专为植物（Plant）设计，提取并对齐 7 个核心分类阶元（去除了植物界较少使用的“总科”）：
界 (kingdom), 门 (phylum), 纲 (class), 目 (order), 科 (family), 属 (genus), 种 (species)。
}
