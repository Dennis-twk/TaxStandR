% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_gbif_Ani.R
\name{search_gbif_Ani}
\alias{search_gbif_Ani}
\title{GBIF 数据库动物/昆虫全量检索与清洗 (GBIF Database Species Search for Animals)}
\usage{
search_gbif_Ani(raw_names)
}
\arguments{
\item{raw_names}{字符型向量 (Character vector)。包含需要清洗和查询的原始动物物种名称。}
}
\value{
返回一个数据框 (Data.frame)。包含标准化后的物种名、GBIF 数据库的 taxid、
完整的 8 级分类阶元信息（包含从 NCBI 跨库补齐的 superfamily），以及追踪数据来源的 \code{source} 列。
}
\description{
本函数专为 GBIF (Global Biodiversity Information Facility) 数据库设计，专门用于检索动物/昆虫数据。
作为流水线的核心模块之一，它集成了极强的抗干扰检索与智能筛选机制。

专属核心机制：
\enumerate{
\item 分类学限定检索：首轮检索强制附加 \code{class = "Insecta"} 参数，极大过滤掉同名的植物或真菌，提高命中准确率；
\item 接受名 (ACCEPTED) 优先：遇到 GBIF 庞大的异名库时，智能锁定官方认证的接受名；
\item 词尾容错映射：智能将 \code{-us}, \code{-um}, \code{-a} 等词尾统一映射对比，挽回大量拼写差异导致的漏网之鱼；
\item 跨库联动补齐：提取 GBIF 查到的科名 (Family)，跨库调用 NCBI 接口补齐总科 (Superfamily) 信息。
}
}
