% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_gbif_Pla.R
\name{search_gbif_Pla}
\alias{search_gbif_Pla}
\title{GBIF 数据库植物物种全量检索与清洗 (GBIF Database Species Search for Plants)}
\usage{
search_gbif_Pla(raw_names)
}
\arguments{
\item{raw_names}{字符型向量 (Character vector)。包含需要清洗和查询的原始植物物种名称。}
}
\value{
返回一个数据框 (Data.frame)。包含标准化后的物种名、GBIF 数据库的 taxid、
完整的 7 级植物分类阶元信息，以及追踪数据来源的 \code{source} 列。
}
\description{
本函数专为 GBIF (Global Biodiversity Information Facility) 数据库设计，专门用于检索植物数据。
具备自动清理乱码、严格匹配以及人工交互复核功能。

针对 GBIF 数据库返回数据繁杂的特点，本模块内置了以下机制：
\enumerate{
\item 全界域检索：去除了动物专用的 \code{class = "Insecta"} 限制；
\item 智能筛选机制：遇到多个同名匹配时，优先捕获 \code{status == "ACCEPTED"} 的标准接受名；
\item 词尾等效容错：内置 4 大拉丁形容词变格映射（如 -us/-um 映射 -a，-is 映射 -e）；
\item 预防空值出现报错与植物降维：去除了总科阶元提取，并在零匹配时输出标准空表，保障流水线稳定。
}
}
